name: mediq_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Core
  cupertino_icons: ^1.0.6
  flutter_riverpod: ^2.6.1  # FIXED (Was 3.0.3)
  go_router: ^14.2.0        # FIXED (Was 17.0.0)
  dio: ^5.7.0               # FIXED (Was 5.9.0)
  flutter_secure_storage: ^9.2.2
  
  # Features
  shared_preferences: ^2.2.3
  sqflite: ^2.3.3+1
  firebase_core: ^3.1.0    # ✅ NEW
  firebase_messaging: ^15.0.0
  firebase_auth: ^5.1.0
  google_fonts: ^6.2.1
  intl: ^0.19.0
  flutter_markdown: ^0.7.7+1
  url_launcher: ^6.3.2
  geolocator: ^12.0.0
  geocoding: ^3.0.0
  image_picker: ^1.2.1
  http_parser: ^4.0.2
  agora_rtc_engine: ^6.3.0
  permission_handler: ^12.0.1
  web_socket_channel: ^3.0.1
  speech_to_text: ^7.3.0
  camera: ^0.11.3
  flutter_image_compress: ^2.4.0
  pdf: ^3.10.0
  printing: ^5.11.0
  path_provider: ^2.1.5
  open_file: ^3.3.2
  image_gallery_saver_plus: ^4.0.1
  flutter_dotenv: ^6.0.1
  supabase_flutter: ^2.12.4
  shimmer: ^3.0.0
  share_plus: ^10.1.4
  flutter_tts: ^4.2.5
  just_audio: ^0.10.5

  # 🔭 Observability
  sentry_flutter: ^8.3.0    # Crash reporting & performance tracing
  posthog_flutter: ^4.0.0   # Product analytics (events, funnels, session recording)
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true
  assets:
    - .env